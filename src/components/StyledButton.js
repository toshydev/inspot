import styled from "styled-components";

const StyledButton = styled.button`
  border-radius: 5px;
  width: 6rem;
  height: 2rem;

  &:hover {
    background: #be4bdb;
    color: white;
    font-weight: bold;
  }
`;

export default StyledButton;
